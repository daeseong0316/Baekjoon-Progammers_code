#include <iostream>
using namespace std;

int main() {
    long long A, B;
    long long result = 0;
    cin >> A >> B;

    result = (A * A) - (B * B);

    cout << result;

    return 0;
}